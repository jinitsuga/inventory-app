extends layout 

block content 

   

        h1= title
        h4 Deleting the following piece from the database:
        ul 
        li #[strong #{piece.name}] 
        li #[strong Stock: #{piece.stock}]

        h5 Are you sure?
        
        form(method='POST' action='')
            input(type='hidden' value=piece._id name='pieceid' required=true)
            button(type='submit') Delete
